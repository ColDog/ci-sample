default_build: &default_build
  pre_test:
    - "echo 'pre test'"
  test:
    - "sleep 5"
    - "echo 'testing'"
  post_test:
    - "echo 'post test'"
  on_success:
    - "echo 'on success'"
  on_failure:
    - "echo 'on failure'"

builds:
  - <<: *default_build
    env:
      name: 'coldog'
      stuff: 'things'
      
    test:
      - "sleep 6"
      - "echo $STUFF"
      - "echo $NAME"

  - <<: *default_build
    test:
      - "sleep 7"

default_build: &default_build
  pre_test:
    - "echo 'pre test'"
  test:
    - "sleep 5"
    - "echo 'testing'"
  post_test:
    - "echo 'post test'"
  on_success:
    - "echo 'on success'"
  on_failure:
    - "echo 'on failure'"

builds:
  - <<: *default_build
    test:
      - "sleep 25"

  - <<: *default_build
    test:
      - "sleep 60"

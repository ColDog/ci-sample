default_build: &default_build
  before:
    - "echo 'pre test'"
  main:
    - "sleep 5"
    - "echo 'testing'"
  after:
    - "echo 'post test'"
  on_success:
    - "echo 'on success'"
  on_failure:
    - "echo 'on failure'"

builds:
  - <<: *default_build
    env:
      name: 'coldog'
      stuff: 'things'
      
    main:
      - "sleep 6"
      - "echo $STUFF"
      - "echo $NAME"

  - <<: *default_build
    main:
      - "sleep 7"
